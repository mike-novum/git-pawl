# TASK-202 — WorkspaceTile: добавить отступ от иконки к размеру репозитория

## Баг
На странице воркспейсов (`src/pages/workspaces/ui/WorkspaceTile.tsx`) в карточке воркспейса текст размера репозитория прижимается к иконке папки слева — не хватает отступа. Нужно добавить визуальный gap между нижним рядом (size слева, FolderOpen справа).

## Что сделать
1. Открыть `src/pages/workspaces/ui/WorkspaceTile.tsx`.
2. В нижнем блоке (`flex items-center justify-between`) для левого элемента со span sizeBytes добавить класс `gap-2` к контейнеру или `pl-1`/`ml-1` к самому span, чтобы текст не прижимался к иконке FolderOpen.
3. Проверить визуально, что между size и FolderOpen иконкой справа есть разумный отступ (≈ 8px).

## Acceptance criteria
- [ ] Текст размера репозитория в карточке воркспейса визуально отделён от иконки FolderOpen.
- [ ] `npm run tsc` + `eslint` проходят без ошибок.

## Зависит от
—

## Статус: DONE — добавлен отступ между размером и иконкой папки

### Что сделано
- Для нижнего ряда карточки добавлен Tailwind-класс `gap-2`.
- Обновлен статус TASK-202 в списке задач.

### Acceptance criteria
- [x] Текст размера репозитория в карточке воркспейса визуально отделён от иконки FolderOpen.
- [x] `npm run tsc` + `eslint` проходят без ошибок.

### Заметки для ревьюера
- Изменены только классы отступов и статусы задачи.