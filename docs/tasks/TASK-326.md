# TASK-326 — Кнопка глобальных настроек: оставить только на главной

## Контекст

Из фидбэка пользователя (roadmap-9, доработка #2):
- Кнопку глобальных настроек оставить только на главном (первом) экране со списком воркспейсов.
- На странице воркспейса и странице репо — убрать эти кнопки.
- На странице воркспейса — переместить в шапку кнопку, открывающую настройки воркспейса.

## Что сделать

### Шаг 1. Анализ текущего состояния

Файлы:
- `src/widgets/app-header/`
- `src/pages/workspace/`
- `src/pages/repository/`
- `src/pages/workspaces/`
- `src/features/global-settings/`

Найти, где используется кнопка открытия глобальных настроек (вероятно, через `useSettingsDialog` или `<SettingsDialog />`).

### Шаг 2. Условный рендер кнопки

Файл: `src/widgets/app-header/ui/AppHeader.tsx` (или где AppHeader):
- Использовать `useLocation()` (react-router-dom) или другую логику, чтобы понять, на какой странице мы.
- Кнопка глобальных настроек показывается только если `route === '/'` (или `/workspaces`).

Альтернативно: кнопка передаётся через props, а в page-component решает — рисовать или нет.

### Шаг 3. Переместить кнопку настроек воркспейса в шапку

Файл: `src/pages/workspace/ui/WorkspacePage.tsx`:
- Если кнопка настроек воркспейса была в боковой панели / drawer — перенести в основной header страницы (рядом с селектором воркспейса).

### Шаг 4. Убрать на странице репо

Файл: `src/pages/repository/ui/RepositoryPage.tsx`:
- Убрать кнопку глобальных настроек (если она там была).

### Шаг 5. Тесты

Файл: `src/widgets/app-header/ui/AppHeader.test.tsx` (новый или обновить):
- Тест: на роуте `/` — кнопка глобальных настроек видна.
- Тест: на роуте `/workspace/123` — кнопка глобальных настроек НЕ видна.
- Тест: на роуте `/repo/...` — кнопка глобальных настроек НЕ видна.

### Шаг 6. Верификация

- `npm run tsc`
- `npx eslint <files>`
- `npm test`
- Storybook / Dev: проверить три страницы.

## Acceptance criteria

- [ ] Кнопка глобальных настроек видна только на главной странице (списка воркспейсов).
- [ ] На странице воркспейса и репо кнопка глобальных настроек НЕ видна.
- [ ] На странице воркспейса кнопка настроек воркспейса в шапке.
- [ ] `npm run tsc` + `npx eslint` — без ошибок.
- [ ] `npm test` — без новых fallов.

## Зависит от
—

## Заметки

- Использовать `useLocation` (react-router-dom v6) — `const location = useLocation(); const showGlobalSettings = location.pathname === '/'`.
- AGENTS.md: стрелочные функции, типы в `types.ts`, без комментариев.

## Статус: DONE — глобальные настройки видны только на главной, кнопка настроек воркспейса перенесена в шапку

### Что сделано
- В `AppHeader` добавлен `useLocation()`: кнопка глобальных настроек рендерится только если путь `/` или `/workspaces`.
- В `AppLayout` убран `hideSettings={isSettings}` (логика полностью перенесена в `AppHeader`).
- Кнопка настроек воркспейса перенесена из `WorkspaceHero` в `AppHeader.rightSlot` через React Router Outlet context (тип `AppLayoutOutletContext` в `shared/lib`).
- `WorkspaceHero` больше не принимает `onSettings` — упрощён `WorkspaceHeroProps`.
- В `AppHeader.test.tsx` добавлены тесты на `/workspaces` (видима), `/workspaces/:id` (скрыта), `/repos/:id` (скрыта), `/settings` (скрыта).

### Acceptance criteria (отметить выполненные)
- [x] Кнопка глобальных настроек видна только на главной странице (списка воркспейсов).
- [x] На странице воркспейса и репо кнопка глобальных настроек НЕ видна.
- [x] На странице воркспейса кнопка настроек воркспейса в шапке.
- [x] `npm run tsc` + `npx eslint` — без ошибок.
- [x] `npm test` — без новых fallов (270 тестов прошли).

### Заметки для ревьюера
- Связь между `WorkspacePage` и шапкой реализована через React Router Outlet context (`setHeaderAction`). Тип вынесен в `shared/lib`, чтобы не нарушать FSD (pages не могут импортировать из app).
- Проверено визуально через Playwright на 4 страницах: `/workspaces`, `/workspaces/:id`, `/repos/:id`, `/settings`.
