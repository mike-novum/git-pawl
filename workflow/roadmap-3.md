# Bugs

- у селекта через который выбирается воркспейс  список не имеет фона из чего его контент сливается с контентом сайдбара, если это на уровне UI компонента так сделано то добавить фон на уровне UI компонента у дропдауна 

- кнопку переключения темы в сайдбаре сделать более простой квадратной с иконками солнца (светлая тема) и месяца (темная тема)