# Доработки по дизайну

1. мне не нравится основной сайдбар по наполнению и в целом общая концепция основного экрана. давай может сделаем вот так:

- сделать отдельный экран для воркспейсов, где воркспейсы будут лежать плитками-карточками с названием и счетчиком количества репозиториев внутри. вверху кнопка Add workspace для добавления новых воркспейсов, и пару кнопок внизу с переключением темы и переходом в настройки

- настройки сделать отдельным экраном со своим сайдбаром и пунктами General, Accounts (аккаунты туда перенести)

- если воркспейсов нет то показывать пользователю красивую какую то заглушку с градиентами с текстом Создайте свой первый воркспейс с кнопкой Создать


- по нажатию на воркспейс открывается страница воркспейса 
- на странице воркспейса должны как то отображаться репозитории которые мы будем добавлять из существующих папок (хотя приложение в целом и без этого умеет сканироавть папку воркспейса) и клонируя по ссылке
- где то должны быть настройки воркспейса (чтоб можно было поставить иконку воркспейсу и сменить на свое название (по умолчанию название папки которую выбрали для воркспейса))
- где то должна быть кнопка удаления воркспейса
- предусмотреть еще группировку репозиториев в воркспейсе (допустим если пользователь внутри папки воркспейса репозитории группирует по папкам то эти папки считаются группами, поддерживает только 1 уровень группировки)


2. когда щелкаю на репозиторий открывается очень странный некрасивый экран, надо как то  сделать этот интерфейс красивее и понятнее для пользователя. там как минимум не хватает кнопки для возврата к ссписку репозиториев (например кнопка с иконкой-шевроном). я хочу открывая репозиторий видеть граф коммитов, список веток, список тегов, кнопки управления (пуш пулл и прочие гит операции), 



> все эти замечания нужно побрейнштормить используя superpowers и frontend-design, как минимум придумать как сделать удобнее и вдруг я предлагаю плохие идеи по редизайну

> предварительно можешь использовать playwright чтобы отскринить все экраны и посмотреть сам то что есть глазами пользователя